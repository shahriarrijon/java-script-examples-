//an expression generates a value, and a statement carries out a complete action. Expressions can be part of statements.
var sum = 2 + 3; // The expression 

var  x; // This is a statement (variable declaration)

if (x > 0) {
    // This is a statement (if statement)
    console.log("x is positive");
}